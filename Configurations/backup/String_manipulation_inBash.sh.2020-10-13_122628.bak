#!/bin/bash
#Shell parameter expansion

# The construct ${parameter#pattern} removes the smallest leftmost part that matches the pattern.

# The construct ${parameter##pattern} removes the largest leftmost part that matches the pattern.

# The construct ${parameter%pattern} removes the smallest rightmost part that matches the pattern.

#  ${parameter%%pattern} stands for the expansion of parameter without the longest rightmost string that matches pattern.